import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  filterContainer: {
    width: '20%',
    borderRadius: 20,
    borderWidth: 1,
    borderColor: 'black',
    alignItems: 'center',
    padding: 4,
  },
  mainContainer: {
    borderWidth: 2,
    borderColor: 'red',
  },
});
